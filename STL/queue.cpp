
queue<type> q;

// Member functions

    st.push();      // O(logN)
    st.pop();       // O(logN)

    st.front();      // O(1)
    st.back();      // O(1)
    
    st.empty();     // O(1)
    st.size();      // O(1)